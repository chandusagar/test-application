// Exercise: Check out while, do..while
for( let i = 0; i < 10; i++ ) {
    console.log( i );
}

const days = [ 'Monday', 'Tuesday', 'Wednesday' ];

for( let idx = 0; idx < days.length; idx++ ) {
    console.log( days[idx] );
}